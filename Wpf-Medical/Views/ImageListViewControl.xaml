<UserControl x:Class="Wpf_Medical.Views.ImageListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate  x:Key="PictureItemTemplate">
            <StackPanel >
                <Image HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Source="{Binding ., Converter={StaticResource ImgConverter}}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ListBox x:Name="ListPictures" ItemsSource="{Binding Path=Pictures}" ItemTemplate="{StaticResource PictureItemTemplate}" />
</UserControl>
