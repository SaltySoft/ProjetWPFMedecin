<UserControl x:Class="Wpf_Medical.Views.PrescriptionListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="PrescriptionItemTemplate">
            <StackPanel Orientation="Vertical">
                <TextBlock TextWrapping="Wrap" Text="{Binding}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ListBox x:Name="ListPrescriptions" ItemsSource="{Binding Path=Prescriptions}" ItemTemplate="{StaticResource PrescriptionItemTemplate}" />
</UserControl>
